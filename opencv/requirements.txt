# Land Logic DRONE-MAPPING-AI — OpenCV Dependencies
# Member 4: OpenCV & Orthomosaic Stitching (feature/opencv)

# Pinned for environment reproducibility; does not restore unexposed Stitcher.setFeaturesFinder Python binding (see Known Limitations in opencv/README.md)
opencv-python>=4.9.0,<5.1.0
numpy>=1.26.0
scikit-image>=0.22.0
imutils>=0.5.4
pillow>=10.2.0
